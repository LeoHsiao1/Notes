name: Deploy

on: [push]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
    - name: git pull
      run: |
        cd /home/github/Notes
        git pull
    - name: Test
      run: |
        curl -I leohsiao.com --connect-timeout 3